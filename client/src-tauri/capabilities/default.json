{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main MyNotes window: core APIs and native notifications.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
