# Production backend URL for the Windows desktop build (loaded by
# scripts/tauri-frontend-build.mjs when running `npm run tauri build`).
# The desktop installer must never be built against http://localhost.
# Web builds (npm run build) are unaffected and use their own environment.
VITE_API_URL=https://mynote-ydld.onrender.com/api